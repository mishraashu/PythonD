from spy_detail import spy_name,spy_age,spy_salutaion
print "hi.."
print "Welcome to the SpyChat"
spy_exist = raw_input("Are you existing spy? Y or N")
spy_exist = str(spy_exist)
if (spy_exist.upper()== 'Y'):
    print 'we have already your details'
    choice = True
    while choice:
        spy_choice = input("Do you want to update status press 1 or if u wana exit press 2")
        if(spy_choice == 1):
            spy_status = raw_input('your status')
        elif (spy_choice == 2) :
            print 'you are log out'
            choice = False
        else :
            print 'wroung choice'
elif (spy_exist.upper()== 'N'):
    spy_name = raw_input("Enter your name")
    if len(spy_name) >= 3:
        print "welcome" + spy_name + ", i like to know about you."
        spy_salutaion = raw_input("what should we call you(Mr. or Ms.)?")
        if (spy_salutaion) > 0 :
            spy_name = spy_salutaion + " " + spy_name
            print "Alright" + spy_name + ", i like to know more about you.."
            spy_ratings = input("please enter your ratings")
            if (spy_ratings) >= 5.0 :
                print 'Expert spy'
            elif (spy_ratings) <=  4.0 :
                print('good spy')
            elif (spy_ratings) <= 3.0 :
                print('bad spy')
            else :
                print('wroung entery')
            spy_age = input("Enter your age")
            if spy_age>20 and spy_age<50 :
                print "you are eligible to be spy"
            else :
                print "you are not eligible for spy"
            print "spy_name is %s and spy_age is %d and rating is %.2f" %(spy_name,spy_age,spy_ratings)
        else :
            print "not valid"
    else :
        print "enter a 3 character name"
else :
    print "invalid"

